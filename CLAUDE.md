# CLAUDE.md

## Commit messages

Commits must follow [Conventional Commits](https://www.conventionalcommits.org/) (`feat:`, `fix:`, `chore:`, etc.). This is enforced locally by a husky `commit-msg` hook running commitlint, and is required for `release-please` to detect version bumps and generate releases.
