# Used by both drizzle-kit (via tests/global-setup.ts) and $env/dynamic/private
# when Vitest runs the app code in 'test' mode. Recreated fresh on every test run --
# see tests/global-setup.ts.
DATABASE_URL=./tests/test.db
